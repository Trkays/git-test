# Используем официальный образ Python
FROM python:3.9

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем текущую папку в контейнер
COPY . .

# Указываем команду для запуска
CMD ["python", "app.py"]